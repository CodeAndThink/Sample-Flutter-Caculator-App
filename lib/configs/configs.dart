import 'package:flutter/material.dart';

class Configs {
  static const Color actionButtonColor = Color(0xFF4B5EFC);
  static const Color subActionButtonColorLightMode = Color(0xFFd2d3da);
  static const Color subActionButtonColorDarkMode = Color(0xFF4e5060);

  static const Color appBarShadowColorLightMode = Color(0xFFd2d3da);
  static const Color appBarShadowColorDarkMode = Color(0xFF4e5060);

  static const Color inactiveTrackColor = Color(0xFF2E2F38);
  static const Color activeTrackColor = Colors.white;

  static const Color inactiveThumbColor = Color(0xFF4E505F);
  static const Color activeThumbColor = Color(0xFFD2D3DA);

  static const Color operatorColorLightMode = Color(0xFF919192);
  static const Color operatorColorDarkMode = Color(0xFF717173);

  static const Color numberColorLightMode = Colors.white;
  static const Color numberColorDarkMode = Color(0xFF2E2F38);

  static const String numberFormat = "#,##0.00";
}
